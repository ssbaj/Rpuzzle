% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid_utils.R
\name{build_property_grid}
\alias{build_property_grid}
\title{위경도 점들을 정사각형 격자로 묶어, 여러 속성(수치/문자)을 함께 집계한다}
\usage{
build_property_grid(lat, lng, cell_size, numeric_props = list(), text_props = list())
}
\arguments{
\item{lat, lng}{같은 길이의 numeric 벡터. 각 점의 위경도.}

\item{cell_size}{격자 한 칸의 크기(m). 다른 데이터셋과 격자 크기를 맞추려면
그 데이터셋에 쓴 것과 같은 값을 지정한다.}

\item{numeric_props}{이름이 붙은 numeric 벡터들의 list(각 벡터 길이는
\code{lat}과 같아야 한다). 칸 안에서 합산되어 같은 이름의 속성으로 담긴다.}

\item{text_props}{이름이 붙은 character 벡터들의 list(각 벡터 길이는
\code{lat}과 같아야 한다). 칸 안에서 관측된 유일값을 정렬 후 ","로
이어붙여 같은 이름의 속성으로 담는다.}
}
\value{
\code{list(features = ..., cell_size = ..., ncols = ..., nrows = ...)}.
  각 feature의 속성에는 \code{numeric_props}/\code{text_props}로 준 이름들과
  더불어, 칸에 집계된 원본 점 개수를 담은 "count"가 포함된다.
}
\description{
\code{\link{build_value_grid}}의 일반화 버전이다. 각 점마다 수치 속성
여러 개(칸 안에서 합산)와 문자 속성 여러 개(칸 안에서 관측된 유일값을
","로 이어붙임)를 함께 붙일 수 있다.
}
\details{
격자 정의(칸 크기, 원점, 칸 개수)는 \code{cell_size}와 점들의 bounding box
만으로 결정된다. 즉 다른 데이터셋(예: 카드매출 geohash 중심점)에도 같은
\code{cell_size}를 지정하면, 서로 다른 원본 데이터로 만든 두 GeoJSON의
격자 칸 크기를 동일하게 맞출 수 있다.
}
