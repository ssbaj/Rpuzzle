% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io_utils.R
\name{run_fetch_script}
\alias{run_fetch_script}
\title{'p유동인구.txt' / 'p카드매출.txt' 같은 요청 스크립트를 실행해 실시간 데이터를 재수집한다}
\usage{
run_fetch_script(script_path, timeout = 6)
}
\arguments{
\item{script_path}{실행할 R 스크립트 경로. \code{NULL}이거나 빈 문자열이면
즉시 실패로 취급한다.}

\item{timeout}{스크립트 실행 중 적용할 네트워크 타임아웃(초).}
}
\value{
\code{list(data = ..., fail_reason = ...)}. 성공 시 \code{data}에
  파싱된 JSON, 실패 시 \code{data = NULL}과 \code{fail_reason} 문자열.
}
\description{
파이썬 버전은 \code{requests.get(...)} 호출로 끝나는 파이썬 스크립트를
\code{exec()}했지만, R 버전에서는 스크립트가 R 코드여야 하며 마지막에
\code{httr::GET(...)} 등으로 만든 \code{response} 객체를 환경에 남겨야 한다.
}
\details{
쿠키 만료/네트워크 오류 등 어떤 이유로든 실패하면 \code{data = NULL}과
실패 사유 문자열을 반환하고, 호출 측이 저장된 원본 파일로 대체(fallback)할
수 있게 한다.
}
