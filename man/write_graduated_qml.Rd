% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid_utils.R
\name{write_graduated_qml}
\alias{write_graduated_qml}
\title{"value" 속성 기준 5단계 분위수 그라데이션 QML 스타일을 저장한다}
\usage{
write_graduated_qml(qml_path, attr = "value", values = NULL, breaks = NULL)
}
\arguments{
\item{qml_path}{저장할 .qml 경로.}

\item{attr}{그라데이션을 적용할 속성 이름.}

\item{values}{격자 셀 값들의 numeric 벡터. \code{breaks}가 없으면 이것으로
분위수 구간을 계산한다.}

\item{breaks}{구간 상한값 5개(오름차순). \code{NULL}이면 \code{values}의
0 초과 값으로 분위수를 계산한다.}
}
\value{
사용된 \code{breaks} (invisible).
}
\description{
같은 이름(\verb{<geojson 파일명>.qml})으로 저장해두면 QGIS가 레이어를 열 때
자동으로 이 스타일을 적용해, 별도 설정 없이 격자가 색깔로 표시된다. 0(빈 칸)은
옅은 회색 반투명으로 별도 처리한다.
}
