% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid_utils.R
\name{estimate_cell_size}
\alias{estimate_cell_size}
\title{점들의 최근접 거리 중앙값으로 격자 한 칸의 크기(m)를 추정한다}
\usage{
estimate_cell_size(lat, lng, value, default = 50)
}
\arguments{
\item{lat, lng, value}{같은 길이의 numeric 벡터.}

\item{default}{추정 실패 시 사용할 기본 셀 크기(m).}
}
\value{
추정된 셀 크기(m), numeric 스칼라.
}
\description{
표본이 너무 적거나 추정이 실패하면 \code{default}(m)를 그대로 사용한다.
}
\details{
전체 점에 대해 결정론적으로 계산한다(무작위 표본을 쓰지 않는다) — 같은
입력이면 언어/실행 환경에 관계없이(파이썬 pypuzzle 포함) 항상 같은 값이
나오게 하기 위함이다. 중앙값도 통계적 평균이 아니라 파이썬의
\code{sorted(dists)[len(dists) // 2]}와 똑같은 규칙(정렬 후 floor(n/2)번째,
0-based)으로 골라 두 언어의 결과를 정확히 일치시킨다.
}
